export default function (options) {
    return {
        getOutput: () => {
        },
        getMetadata: () => {
        },
        defaultExtension: "html",
        getLinks: () => {
        }
    };
}
